#include<stdio.h>
void main()
{

int x,rem;
int rev=0;
printf("enter a no  ");
scanf("%d",&x);
while(x!=0)
{
    rem=x%10;
    rev=rev*10+rem;
    x=x/10;
    }
    printf("reverse no =%d",rev);
    }
